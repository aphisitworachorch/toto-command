#This is Toto inheriter for all but somethings i don't want to say

 #include <stdio.c>
 #include <heart.c>
 #include <birthday.c>
 { 
  if toto.exec use <birthday.c>
  else fivesalashfive.exec { sensorhal;}
  
 }

  printf("สิ่งที่กูช่วยพวกมึงไว้ไม่มีึความหมายแล้วหรอ ? กูก็น้อยใจเป็นเหมือนกันน่ะ ขอร้องอย่ามาช่วยแล้วด่ากูทีหลัง ")

  if fivesalashfive.exec; respond {set1.c}

 }

set1.c {hal,sensoring}
 
 {

